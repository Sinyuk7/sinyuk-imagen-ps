{
  "name": "@imagen-ps/app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit --project tsconfig.build.json && pnpm run build:uxp && pnpm run build:chrome",
    "build:uxp": "vite build --config vite.uxp.config.ts",
    "build:chrome": "vite build --config vite.chrome.config.ts",
    "dev": "vite build --config vite.uxp.config.ts --watch",
    "test": "vitest run",
    "test:chrome-e2e": "node tests/chrome-e2e/run.mjs",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@imagen-ps/application": "workspace:*",
    "@imagen-ps/foundation": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "happy-dom": "^20.10.3",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
