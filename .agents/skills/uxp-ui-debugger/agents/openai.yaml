interface:
  display_name: "UXP UI Debugger"
  short_description: "Confirmed-UI Photoshop UXP debugger"
  default_prompt: "Use $uxp-ui-debugger only when the user explicitly invokes it, explicitly asks for this workflow, or a manually-invoked $uxp-issue-rca run has handed off a pure UI finding, then debug the confirmed Photoshop UXP UI bug."
