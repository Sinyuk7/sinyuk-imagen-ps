# AGENTS.md

## Hard Rules

- `@imagen-ps/providers` stays the provider adapter layer.
- No app/session state, job lifecycle ownership, host IO, or settings persistence.
- No dependency on surface modules.
